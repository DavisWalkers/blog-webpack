import { NavBar } from './NavBar';
import { Containter, HeaderElement } from './styles';

export const Header = () => {
  return (
    <HeaderElement>
      <Containter>
        <NavBar />
      </Containter>
    </HeaderElement>
  );
};